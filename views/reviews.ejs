<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <h1><%= title %></h1>

    <% if (reviews && reviews.length > 0) { %>
        <% reviews.forEach(review => { %>
            <div>
                <h2><%= review.title || 'Kein Titel' %></h2>
                <p>Autor: <%= review.author || 'Unbekannt' %></p>
                <p>Bewertung: <%= review.rating || 0 %> Sterne</p>
                <p>Produkt: <%= review.product || 'Unbekannt' %></p>
                <p><%= review.text || 'Kein Text verfügbar' %></p>
            </div>
            <hr>
        <% }); %>
    <% } else { %>
        <p>Keine Rezensionen gefunden.</p>
    <% } %>
</body>
</html>
